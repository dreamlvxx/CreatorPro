<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dream.create">

    <application
        android:name=".framework.CreatorApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CreatorPro">
        <activity android:name=".business.ui.view.activity.MainActivity"/>
        <activity
            android:name=".business.ui.view.activity.home.HomeActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".business.ui.view.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.MONKEY" />
            </intent-filter>
        </activity>
        <activity
            android:name=".business.ui.view.activity.home.AddCommentActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>